spring.application.name=progettopersonale

# La password del database NON deve mai stare nel codice sorgente/versionato.
# Impostala come variabile d'ambiente DB_PASSWORD (IDE run config, terminale, o .env locale ignorato da git).
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres.pogumhkydjnuwsdgwtxo}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8081
